// ---------------------------------------------------------------------------
// Shared constants — centralised so magic numbers live in one place
// ---------------------------------------------------------------------------

/** Active-session polling interval (ms) */
export const SESSION_POLL_INTERVAL_MS = 3_000;

/** Sessions-list polling interval (ms) */
export const SESSIONS_LIST_POLL_INTERVAL_MS = 5_000;

/** Default page size for sessions list */
export const SESSIONS_PAGE_SIZE = 100;
