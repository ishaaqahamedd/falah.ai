# Root .env for docker-compose
# Copy this to .env and fill in your values:
#   cp .env.example .env

# Your LiveKit server URL — used by the frontend container at build time
LIVEKIT_URL=wss://your-app.livekit.cloud
